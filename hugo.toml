baseURL = 'https://ivanluminaria.com/'
# baseURL = "https://ilum75IDB.github.io/www.luminaria.it/"
title = "Ivan Luminaria"
theme = "congo"
locale = 'it'

defaultContentLanguage = "it"
defaultContentLanguageInSubdir = true

enableRobotsTXT = true

# Taxonomies: solo "tags" (rimossa "categories" che generava /categories/X duplicate delle /posts/X)
[taxonomies]
  tag = "tags"

[outputs]
  home = ["HTML", "RSS", "JSON"]
