# Handoff sessione — 2026-09-12

| Campo | Valore |
|---|---|
| **Data** | 2026-09-12 |
| **Sequenza in giornata** | 1° |
| **Supersede** | — |
| **Branch operativa** | `claude/work-in-progress` |
| **Branch target** | `main` |
| **Commit HEAD su main (pre-handoff)** | `979fa42` |
| **Stato sincronizzazione** | ✅ tutti i branch allineati a origin dopo rewrite |
| **Tema sintetico** | Allineamento repo iMac alla storia riscritta con `git filter-repo` pushata da MacBook (rimozione trailer `Co-Authored-By`/`Claude-Session` + hostname reali + UUID cluster) |

---

## 1. Cosa è stato costruito

Nessun cambiamento di contenuto: la sessione è puramente di **housekeeping git**. L'output è che questo Mac (iMac) ora vede esattamente la stessa storia che vive su `origin` dopo il rewrite fatto ieri (2026-09-11) dal MacBook con `git filter-repo`.

Rewrite fatto sul MacBook (per contesto — non su questo Mac):

- **Trailer rimossi** in tutti i messaggi di commit: `Co-Authored-By: Claude...` e `Claude-Session: ...`
- **Identificativi reali rimossi** dal contenuto dei file in tutta la storia:
  - Hostname cluster: `pglsdb01c`, `pglsdb03c`, `pglsdb04c` → sostituiti con `mysql-node-01/02/03`
  - UUID cluster: `ea048e66...` → rimosso
- **Push forzato** su tutti e 3 i branch: `main`, `publication-workflow`, `claude/work-in-progress`

## 2. Cosa è stato escluso (e perché)

- **`publication-workflow` non è stato toccato oltre al reset --hard di allineamento**. Ha una divergenza strutturale pre-esistente da `main` (fork da `cf474cf`) — è per design (branch operativo WEBO per scheduling articoli, con cherry-pick atomico su main come da CLAUDE.md). Non ho tentato merge né riallineamento perché fuori scope di una sessione di housekeeping.
- **Nessun cherry-pick o replay dei 940 commit locali** era necessario: il contenuto dei file su `origin/main` (riscritto) coincide con quello vecchio salvo hostname/UUID rimossi — non c'è lavoro da recuperare.
- **Nessun `git pull` è stato eseguito** (era esplicitamente vietato dall'utente: avrebbe rifuso le due storie divergenti reintroducendo trailer e hostname).

## 3. Pulizie / Modifiche applicate

Sequenza operativa eseguita su iMac (working tree pulito all'ingresso, nessuno stash):

1. `git fetch origin --prune --tags --force` → forced update su tutti e 3 i branch remoti (atteso dopo rewrite)
2. Verifica commit locali non su origin: **952 commit unici** distribuiti sui 3 branch (940 main / 880 pub-workflow / 871 WIP) — tutti coincidenti col rewrite salvo SHA
3. Verifica assenza di branch feature locali extra: OK, solo i 3 attesi
4. **Approvazione esplicita utente al reset --hard** (via `AskUserQuestion`)
5. `git reset --hard origin/main` (sul branch corrente `main`) — ~246 file riscritti nel working tree
6. `git branch -f publication-workflow origin/publication-workflow`
7. `git branch -f claude/work-in-progress origin/claude/work-in-progress`
8. Verifica finale per branch (`git checkout` su `publication-workflow` e `claude/work-in-progress`): working tree pulito, HEAD allineato

## 4. Sorgenti del repo modificati (autorizzati esplicitamente)

Nella sessione di housekeeping:

- **Nessuno** (il reset --hard non produce commit, solo sposta gli HEAD dei branch locali sugli HEAD dei rispettivi `origin/`).

Nel commit di handoff (questo):

- `docs/handoff/HANDOFF_2026-09-12.md` — nuovo file
- `docs/handoff/README.md` — nuova riga in indice storico

## 5. Tool prodotti

Nessuno. Sessione operativa su git, nessun tool nuovo o script aggiornato.

## 6. Comunicazioni preparate

Nessuna.

## 7. Stato git

Post-rewrite, prima del commit di handoff:

| Branch | Locale | Origin | Stato |
|---|---|---|---|
| `main` | `979fa42` | `979fa42` | ✅ allineato |
| `claude/work-in-progress` | `979fa42` | `979fa42` | ✅ allineato (stesso HEAD di main) |
| `publication-workflow` | `26a2849` | `26a2849` | ✅ allineato (divergente da main by design) |

**Verifiche finali post-rewrite** (comandi + risultati attesi/ottenuti):

- `git log --branches --not --remotes --oneline | wc -l` → `0` ✅
- `git log --all --format=%B | grep -c -iE '^(co-authored-by|claude-session)'` → `0` ✅
- `git rev-list --all | xargs git grep -l -iE 'pglsdb|ea048e66' -- | wc -l` → `0` ✅

## 8. 🔓 Punti aperti

### 🟡 Da valutare — `publication-workflow` post-rewrite

- **Fatto**: `publication-workflow` è stato allineato a `origin/publication-workflow` (26a2849), che è il risultato del rewrite di `git filter-repo` applicato anche a quel branch.
- **Situazione strutturale invariata**: `publication-workflow` resta divergente da `main` con fork base `cf474cf` (era divergente anche prima del rewrite — è il modello operativo del progetto, non un problema).
- **Da confermare quando il workflow editoriale ripartirà**: il primo commit editoriale post-rewrite dovrebbe verificare che WEBO (l'orchestratore in `~/Development/APPLICAZIONI/My-Web-Site/web-orchestrator/`) non abbia SHA "vecchi" memorizzati in tabelle locali che referenzino commit ora inesistenti. Se qualche entry `commit_sha` in DB WEBO punta a un SHA della vecchia storia, andrà rigenerata dal contenuto (o marcata come pre-rewrite).

### 🟡 Da controllare — altri Mac con questo repo

- Se **altri cloni** di questo repo esistono su altri Mac oltre a iMac (questo, ora allineato) e MacBook (che ha fatto il rewrite), vanno riallineati con la stessa procedura eseguita qui (fetch --force + reset --hard).
- Verificare se il MacBook stesso ha già completato l'allineamento locale al termine dell'operazione `filter-repo` di ieri.

### 🟢 Idea strategica — anonimizzazione preventiva nei nuovi articoli

- Il rewrite è stata una toppa retroattiva. Il commit `6562850 docs(briefing): l'anonimizzazione diventa un divieto globale, non una sezione opzionale` (già su `main` post-rewrite) formalizza già la regola: **niente hostname reali, UUID reali, IP reali, nomi cluster reali in commit o nel testo degli articoli**.
- Verificare che questa regola sia recepita anche in `docs/AI_CONTENT_GUIDELINES.md` (versione EN) oltre che nella versione IT, così ogni futura scrittura via skill `blog-idea-to-issue` la applichi sistematicamente.

## 9. Memorie persistenti salvate

Nessuna memoria salvata in questa sessione. Il flusso operativo (rewrite → fetch --force → reset --hard) è un pattern noto, non serve memorizzarlo. L'evento specifico è tracciato in questo handoff.

## 10. Brief minimo per riavviare il lavoro

**Contesto in una frase**: il repo `ivanluminaria.com` ha una nuova storia (post `git filter-repo`, 2026-09-11) su tutti e 3 i branch — iMac riallineato il 2026-09-12, `main` = `claude/work-in-progress` = `979fa42`, `publication-workflow` = `26a2849`.

**Per una nuova sessione di lavoro sul repo**:

```bash
cd ~/Development/APPLICAZIONI/My-Web-Site/ivanluminaria.com
workwww
git status -sb
git log -1 --format='%h %s'
```

Se lo status mostra "up to date with origin/<branch>" e l'HEAD è quello atteso (vedi tabella §7), si può ripartire con normale lavoro (nuovo articolo, fix, ecc.). Se emerge divergenza, verificare prima se è arrivato un nuovo rewrite dal MacBook (raro, ma possibile) prima di fare `git pull`.

**Regola da tenere presente**: qualsiasi hostname/UUID reale di cluster produzione (Poste Italiane in primis) va **anonimizzato subito** in commit e articoli — vedi commit `6562850` per la regola aggiornata.

*Handoff creato: 2026-09-12, fine sessione di housekeeping post-rewrite storia git.*

---

## 11. Post-scriptum — chiusura pubblicazione 2026-09-13

Il commit di questo handoff (`f71f4c7`) è stato pushato su origin il **2026-09-13**, non il giorno stesso della sessione, perché al momento del `git push` iniziale il **token `gh` sull'iMac era scaduto** (`Invalid username or token. Password authentication is not supported for Git operations.`). Il commit è rimasto in coda locale una notte in attesa di re-auth.

**Riautenticazione fatta il 2026-09-13** con `gh auth login -h github.com` (flusso web-based, one-time code). Il nuovo token è salvato nel keyring macOS. Il credential helper git era già configurato correttamente (`!/usr/local/bin/gh auth git-credential`) e ha ripreso a funzionare subito.

**Stato finale post-push** (2026-09-13, verifica locale + remoto):

| Branch | Locale | Origin | Stato |
|---|---|---|---|
| `main` | `f71f4c7` | `f71f4c7` | ✅ allineato |
| `claude/work-in-progress` | `f71f4c7` | `f71f4c7` | ✅ allineato (stesso HEAD di main) |
| `publication-workflow` | `26a2849` | `26a2849` | ✅ allineato (divergente da main by design, non toccato) |

**Nuovo punto aperto emerso** (aggiunto anche in §8 idealmente, ma minore):

- **Scadenza periodica token `gh`**: quando il push fallisce con `Password authentication is not supported for Git operations`, la causa più probabile è token `gh` scaduto — soluzione documentata: `gh auth login -h github.com` (flusso web con `-w`, opzione `-s repo` per push su repo privati). Il credential helper è già configurato in modo persistente, non serve rifare `gh auth setup-git`.

*Post-scriptum aggiunto: 2026-09-13, chiusura sessione dopo push finale riuscito.*
