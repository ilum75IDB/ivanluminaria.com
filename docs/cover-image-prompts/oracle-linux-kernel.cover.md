# Cover image prompt — oracle-linux-kernel.cover.jpg

## Articolo di riferimento

- **Slug**: `oracle-linux-kernel`
- **Sezione**: `oracle`
- **Titolo IT**: *Oracle su Linux: i parametri kernel che nessuno configura*

## Descrizione della scena

Inquadratura cinematografica all'interno di una **sala server in penombra**, taglio orizzontale 3:2. Sul lato destro, un **rack di server** verticali a tutta altezza, con file di **LED rossi e verdi** accesi (i dischi e gli stati di rete), pannelli neri con maniglie metalliche, fascio di cavi neri che scendono lungo il bordo del rack. La luce dominante è **rosso-arancione**, fredda e tecnica, da datacenter notturno.

In primo piano, su una **scrivania scura** parzialmente illuminata, un **laptop aperto** rivolto verso lo spettatore. Lo schermo del laptop mostra una **shell Linux** con testo verde-fosforo su sfondo nero: si leggono righe tipo `kernel.pid_max = 32768`, `vm.swappiness = 10`, `fs.inotify.max_user_watches`, `MemTotal: 32835180`, `SwapTotal: 20971516`, `SwapFree: 20917584` — il classico output di `sysctl` e `/proc/meminfo` che ogni DBA Oracle controlla a inizio assessment.

Vicino al laptop, uno **smartphone con lo schermo spento** appoggiato sul piano, leggermente in primo piano. L'atmosfera è notturna, da intervento in produzione: bagliore verde della shell che si scontra col rosso del rack, riflessi sulla scrivania, dominanti **verde-fosforo e rosso scuro**. Texture leggermente granulosa, sensazione di "indagine paziente" — qualcuno che legge prima di agire.

## Metafora visiva

Il problema non è Oracle, è quello che c'è sotto. Il rack a destra è il "regno visibile" (il database, le query, le lamentele degli utenti), il laptop a sinistra è il "regno invisibile" che pochi guardano (il kernel, le Huge Pages, lo swappiness). La cover mette i due piani sulla stessa scrivania e li fa parlare: la performance vera si decide sul terminale che nessuno apre, non sul rack che tutti guardano.

## File output

- **Nome file**: `oracle-linux-kernel.cover.jpg`
- **Path di destinazione**: `content/posts/oracle/oracle-linux-kernel/oracle-linux-kernel.cover.jpg`
- **Formato**: JPG, ratio 3:2 (coerente con il sistema editoriale del blog)
